# ==============================================================================
# SAHAAY Client (Vite Frontend) Environment Configuration
# ==============================================================================

# Backend API endpoint URL (Used by axios/fetch in browser)
VITE_API_BASE_URL=http://localhost:5000/api

# Default UI language code (e.g. en, hi, ta, te, mr, bn, etc.)
VITE_DEFAULT_LANGUAGE=en
